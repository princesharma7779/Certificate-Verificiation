Certificate Verification System is a Spring Boot-based web application that
verifies certificates using a unique Certificate ID. It retrieves certificate
details such as student name, course, and issue date from a MySQL database using 
Spring Data JPA (Hibernate) and displays the verification result through a Thymeleaf-based user interface.
The project follows the MVC architecture and demonstrates 
CRUD-ready backend development with database integration.
